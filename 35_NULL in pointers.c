#include<stdio.h>
int main()
{
    int *p=NULL;//null is using for empty pointer variable
    //null is using statement ,without hang for project
    if (p==NULL)
    {
        printf("\nP is Null ");
    }
    return 0;
}
